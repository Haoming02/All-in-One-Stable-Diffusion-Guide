# Helper Scripts
Some `python` scripts that ease the process of training models.

### Insert.py
- Insert a tag at the start of the prompt for all text files in a folder. Useful for adding the **trigger words**.
- **Usage:** `>pyton Insert.py <folder path> <tags>`
